{
  "extends": "claude-code",
  "meta": {
    "name": "cc-luphaz",
    "version": "0.2.0",
    "description": "Claude Code for luphaz: built-in claude-code + rw ~/.dotfiles + custom network allowlist. The built-in claude-code profile already grants rw to the whole $HOME/.claude tree and $HOME/.claude.json — we don't redeclare those here."
  },
  "filesystem": {
    "allow": [
      "$HOME/.dotfiles"
    ]
  },
  "network": {
    "allow_domain": [
      "docs.anthropic.com",
      "docs.claude.com",
      "platform.openai.com",
      "learn.microsoft.com",
      "developer.hashicorp.com",
      "docs.datadoghq.com",
      "gitlab.ddbuild.io"
    ]
  }
}
