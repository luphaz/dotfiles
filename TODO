OK VSCODE config (json)
  OK install specific font MesloLGS NF
OK VSCODE extensions
OK ITERM config
OK fonts
bitbar config (might miss a bashrc to expose sensitive env vars)
alfred plugins and configuration whenever not sensitive
